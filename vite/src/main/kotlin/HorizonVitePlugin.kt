package dev.triumphteam.horizon.vite

import org.gradle.api.Plugin
import org.gradle.api.Project

public class HorizonVitePlugin : Plugin<Project> {

    override fun apply(project: Project): Unit = with(project) {

    }
}
